# Server Configuration
# Default port for development and Docker deployment
PORT=8286
HOST=0.0.0.0

# API Base Path
# Configurable base path for all API endpoints (default: /api)
# Useful when hosting behind a proxy or at a subpath
# Examples: /api, /api/v1, /ephemera/api
API_BASE_PATH=/api

# HTML Base HREF (optional)
# Sets the <base href="..."> tag in the HTML for correct relative path resolution
# Useful when embedding in an iframe or hosting at a subpath
# Must end with a trailing slash
# Example: If hosting at https://example.com/ephemera/, set HTML_BASE_HREF=/ephemera/
# Leave empty to disable (default)
HTML_BASE_HREF=

# AA API
AA_API_KEY=your_secret_key_here
AA_BASE_URL=yourarchiveurl.org

# Paths
# Docker: Use /app/books, /app/downloads, /app/data/database.db
# Local: Use your preferred local paths
INGEST_FOLDER=/path/to/final/books
DOWNLOAD_FOLDER=./downloads
DB_PATH=./data/database.db

# Download Settings
MAX_CONCURRENT_DOWNLOADS=1
RETRY_ATTEMPTS=3
REQUEST_TIMEOUT=30000

# Slow Download Settings (optional)
# FlareSolverr is used for slow download fallback when API key is missing or quota exhausted
# Default: http://localhost:8191 (or http://flaresolverr:8191 in Docker)
FLARESOLVERR_URL=http://localhost:8191

# Alternative Download Source (optional)
# If set, LG fast download will be attempted before falling back to slow servers
# Leave empty to disable this feature
# li, bz, etc.
LG_BASE_URL=

# Cache Settings (optional)
SEARCH_CACHE_TTL=300

# Booklore Integration
# Configure Booklore via the API at /api/booklore/settings
# Environment variables are no longer used for Booklore configuration
